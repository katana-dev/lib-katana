# Defines which libraries to create.
lib_LTLIBRARIES = libkatana.la

# Version and sources for libkatana.
libkatana_la_LDFLAGS = -version-info $(LIBKATANAVERSION)
libkatana_la_SOURCES = \
sysex.h	sysex.c

# Public headers needed to use libraries.
#include_HEADERS = \
#example.h
